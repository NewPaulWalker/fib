# fib

一个使用 C 语言实现的斐波那契数列示例程序，输入 `n`（从标准输入读取）后输出第 `n` 项结果。

## 构建与运行

```bash
gcc -Wall -Wextra -Werror fib.c -o fib
echo 10 | ./fib
```

## 说明

- 程序采用非递归（迭代）方式计算。
- 输入非法或越界时会输出错误信息并返回非零退出码。
- 仓库历史中包含了用于演示的缺陷注入提交。
